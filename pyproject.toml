[project]
name = "nanolab-workspace"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "packages/nanolab",
    "packages/workflow-tasks",
    "packages/tui-toolkit",
]
