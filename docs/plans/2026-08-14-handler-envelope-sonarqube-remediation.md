# Handler Envelope and SonarQube Remediation Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Remove the correctness gaps found in `handler-envelope-container`, resolve every actionable SonarQube finding, and leave no untriaged finding in a fresh local scan.

**Architecture:** Fix the workflow contract at its shared Sonata boundary first, then repair source-root/test isolation in NanoLab. Process Sonar findings in risk order: type/security and exception correctness, cognitive complexity, then mechanical cleanup. Intentional local HTTP endpoints are reviewed individually and annotated locally; no global Sonar rule is disabled.

**Tech Stack:** Python 3.12, pytest, Ruff, basedpyright, Sonata, Docker, SonarQube Community 26.7.

---

## Baseline

Fresh scan from `main` at `5de230a`:

| Package | Total | High | Medium | Low |
|---|---:|---:|---:|---:|
| nanolab | 78 | 30 | 10 | 38 |
| sonata-tasks | 49 | 26 | 17 | 6 |
| tui-toolkit | 4 | 3 | 1 | 0 |
| **Total** | **131** | **59** | **28** | **44** |

Rule inventory: S5332 35, S3776 23, S1192 16, S5754 10, S5655 5,
S5886 5, S6353 5, S107 4, S5958 4, S1542 3, S3358 3, S5443 3,
S6796 3, S7519 3, S2068 2, and one each of S108, S1186, S3699,
S5713, S5843, S6792, S8495.

The complete machine-readable baseline is generated by `./scripts/sonar.sh`
at `.scannerwork/issues.json`; do not commit that generated file.

### Task 1: Make API-envelope expectations distinguish unchecked from null

**Files:**
- Modify: `packages/sonata-tasks/src/sonata_tasks/http_function.py:297-402`
- Modify: `packages/nanolab/src/nanolab/plans/validate.py:110-147`
- Test: `packages/sonata-tasks/tests/test_invocation.py`
- Test: `packages/nanolab/tests/plans/test_validate.py`

1. Add failing tests where a plain response unexpectedly contains non-null
   `statusCode`, `headers`, or `encoding`; prove the current `None` defaults skip
   those comparisons.
2. Introduce one private `_UNSET` sentinel and use it as the default for optional
   API expectations. Compare every field whose expectation is not `_UNSET`,
   including an expected value of `None`.
3. Set the plain and five header/body probe expectations explicitly to
   `status_code=None`, `api_headers=None`, and `encoding=None`.
4. Run:

   ```bash
   uv run --locked --all-packages --all-groups pytest -c packages/sonata-tasks/pyproject.toml packages/sonata-tasks/tests/test_invocation.py
   NANOFAAS_ROOT=/path/to/nanofaas uv run --locked --all-packages --all-groups pytest -c packages/nanolab/pyproject.toml packages/nanolab/tests/plans/test_validate.py
   ```

5. Commit: `fix: distinguish absent envelope expectations`.

### Task 2: Split HTTP contract parsing into testable helpers

**Files:**
- Modify: `packages/sonata-tasks/src/sonata_tasks/http_function.py:314-424`
- Test: `packages/sonata-tasks/tests/test_invocation.py`

1. Add failing focused tests for malformed status lines, outer-header mismatch,
   API-field mismatch, invalid base64, and media types with parameters.
2. Extract only four private functions from the nested verifier:
   `_parse_contract_response`, `_verify_outer_headers`,
   `_verify_api_envelope`, and `_verify_decoded_output`.
3. Treat HTTP header names case-insensitively. For `Content-Type`, compare the
   normalized media type and accept valid parameters such as
   `application/json; charset=utf-8`; retain exact comparison for every other
   header value.
4. Replace repeated `Content-Type: application/json` strings with one module
   constant. This resolves workflow findings S3776 and S1192 without adding a
   new public abstraction.
5. Run the Sonata invocation tests, Ruff, basedpyright, then commit:
   `refactor: isolate HTTP envelope verification`.

### Task 3: Use one nanoFaaS source root throughout planning

**Files:**
- Modify: `packages/nanolab/src/nanolab/plans/functions.py:95-114`
- Modify: `packages/nanolab/src/nanolab/plans/validate.py:170-180`
- Modify callers found by `rg 'resolve_function\(' packages/nanolab/src`
- Test: `packages/nanolab/tests/plans/test_validate.py`
- Test: relevant loadtest/offload plan tests

1. Add a failing test with `repo_root` pointing to checkout A while
   `NANOFAAS_ROOT` points to checkout B; assert manifests and names come from A.
2. Add `source_root: Path | None` to `resolve_function` and pass it to
   `resolve_function_definition`.
3. Pass each plan builder's existing repository root through every call; do not
   introduce another global or discovery mechanism.
4. Refactor `_build_argv` to one final return value so all branches satisfy
   S8495 while retaining byte-for-byte argv values.
5. Run all NanoLab plan tests and commit: `fix: resolve functions from plan source`.

### Task 4: Make workflow tests isolated and jointly collectible

**Files:**
- Rename: `packages/sonata-tasks/tests/test_validate.py` to
  `packages/sonata-tasks/tests/test_validate_workflow.py`
- Modify: `packages/nanolab/tests/plans/test_validate.py:223-236`

1. Reproduce the current combined pytest `import file mismatch`.
2. Rename the Sonata module; do not change pytest import configuration globally.
3. Replace the `nanofaas_checkout` fixture with `nanofaas_root` in the catalog
   coverage test so it reads the same archived commit used to compile plans.
4. Assert the three documented exclusions exist in the source catalog before
   subtracting them, preventing stale exclusion names from passing silently.
5. Run both test modules in one pytest command and commit:
   `test: isolate validate workflow coverage`.

### Task 5: Remove the unsafe fixed queue-summary path

**Files:**
- Modify: `packages/sonata-tasks/src/sonata_tasks/validate.py:216-235`
- Test: `packages/sonata-tasks/tests/test_validate_workflow.py`

1. Add a failing plan assertion that the k6 summary does not target a shared
   absolute `/tmp` filename.
2. Use a run-local relative filename under the task working directory. Do not
   add a temp-file abstraction: k6 owns creation and Sonata already controls cwd.
3. Run the validate workflow tests and confirm S5443 disappears for this file.
4. Commit: `fix: keep queue summaries in the workflow directory`.

### Task 6: Correct type-contract findings before refactoring

**Files:**
- Modify and test the five S5655 sites:
  - `packages/nanolab/src/nanolab/plans/release.py:329,343,432`
  - `packages/sonata-tasks/src/sonata_tasks/http_function.py:463`
  - `packages/sonata-tasks/src/sonata_tasks/provisioning/bootstrap.py:114`
- Modify and test the five S5886 sites:
  - `packages/nanolab/src/nanolab/plans/cli.py:120`
  - `packages/nanolab/src/nanolab/plans/offload_loadtest.py:182`
  - `packages/sonata-tasks/src/sonata_tasks/components/bootstrap.py:241,255`
  - `packages/tui-toolkit/src/tui_toolkit/context.py:43`

1. For each site, write or strengthen a test that exercises the returned value
   and exact downstream type.
2. Inspect the callee signature and fix the caller or annotation at the boundary;
   do not silence basedpyright or add casts until runtime type is proven correct.
3. Run the affected package suite and basedpyright after each package.
4. Commit separately per package so any behavior change is independently
   revertible.

### Task 7: Narrow exception handling at all ten S5754 sites

**Files:**
- `packages/nanolab/src/nanolab/release/build.py:722`
- `packages/nanolab/src/nanolab/release/resources.py:401`
- `packages/nanolab/src/nanolab/release/secrets.py:161`
- `packages/nanolab/src/nanolab/tui/workflow_controller.py:101,109`
- `packages/sonata-tasks/src/sonata_tasks/archive.py:127`
- `packages/sonata-tasks/src/sonata_tasks/compensation.py:27`
- `packages/sonata-tasks/src/sonata_tasks/provisioning/environment.py:111`
- `packages/sonata-tasks/src/sonata_tasks/registry_tunnel.py:53,83`

1. At each boundary, add a failing test proving expected operational failures are
   translated while programming errors still escape.
2. Replace `except Exception` with the exact exceptions raised by the immediately
   called API, or re-raise after cleanup when broad catching is required by a
   lifecycle boundary.
3. Remove the redundant exception alternative at
   `sonata_tasks/registry.py:25` (S5713) in the same exception-focused batch.
4. Run affected suites and commit per package.

### Task 8: Resolve security and credential findings explicitly

**Files:**
- `packages/nanolab/src/nanolab/release/secrets.py:16-17`
- `packages/sonata-tasks/src/sonata_tasks/cosign.py:130,135`
- Tests adjacent to both modules

1. Reproduce permissions for secret directories and files; add tests requiring
   owner-only modes and atomic creation before changing paths (remaining S5443).
2. Fix unsafe creation at the shared helper, not each caller.
3. Inspect the two S2068 values. If they are real embedded credentials, move them
   to environment/input boundaries. If they are command vocabulary or variable
   names, add a narrow `NOSONAR` explanation on those two lines only.
4. Run security tests and rescan these components before committing.

### Task 9: Reduce the remaining 22 cognitive-complexity findings

**Files:**
- NanoLab: `cli/execution.py:39,122,307`, `cli/product.py:334`,
  `functions/catalog.py:132`, `plans/loadtest.py:177`, `tui/app.py:294`,
  `tui/event_aggregator.py:96,146`, `tui/workflow_controller.py:22`
- Sonata: `archive.py:14`, `buildx.py:15`, `components/bootstrap.py:221`,
  `cosign.py:47`, `http_function.py:61`, `loadtest/prometheus.py:42`,
  `metrics.py:80`, `platform.py:252`, `process.py:58`,
  `provisioning/environment.py:81`, `registry.py:29`, `registry_tunnel.py:15`

For each function:

1. Freeze existing behavior with one focused characterization test.
2. Extract only cohesive parsing, validation, or branch-selection helpers until
   the function is at or below 15. Do not add classes or strategy abstractions.
3. Run the smallest affected suite after every extraction.
4. Commit in small batches by module family, then rescan to verify all 23 total
   S3776 findings—including Task 2's contract verifier—are gone.

### Task 10: Apply mechanical, behavior-preserving Sonar fixes

**Rules and files:**
- S1192 remaining 15: use private constants only inside the reporting module;
  do not centralize unrelated protocol strings.
- S6353 (5): `release/versioning.py`, `metrics.py`, `vm/azure.py`; replace
  equivalent explicit ASCII classes only when Unicode `\w` semantics are valid.
- S7519 (3): `devtools/package_report.py:45-47`; use `dict.fromkeys` and retain
  insertion order.
- S3358 (3): `plans/loadtest.py`, `release/environment.py`,
  `components/helm.py`; replace nested ternaries with named assignments.
- S1542 (3): `vm/adapters.py`; these are factory functions intentionally named
  like adapters—rename to snake_case and update all imports.
- S6796 (3), S6792 (1): migrate only the reported generic functions/classes to
  Python 3.12 type-parameter syntax.
- S5843 (1): split the metrics regex into named fragments.
- S108/S1186/S3699: remove the empty block, document the intentional no-op, and
  stop consuming the return of a void function.

For every rule, run a RED characterization test where behavior can change,
apply the smallest edit, then run Ruff, basedpyright, and the owning package's
tests. Commit by rule, not as one bulk cleanup.

### Task 11: Review thresholds and intentional local HTTP endpoints

**Files:**
- S107 sites: `plans/loadtest.py`, `plans/release_phases.py`, `cosign.py`
- S5958 test sites reported by Sonar
- All 35 S5332 occurrences listed in `.scannerwork/issues.json`

1. For S107, introduce a request dataclass only where parameters form a stable
   domain object; otherwise add one narrow rationale because wrapping arguments
   solely to satisfy a threshold makes the code harder to call.
2. Replace generic `pytest.raises(Exception)` at the four S5958 test sites with
   the exact exception already emitted by production code.
3. Inspect every S5332 text range. Preserve HTTP only for loopback, Docker/K8s
   internal service names, health probes, or SSH-tunnel endpoints. Add an inline
   `NOSONAR` reason to those exact expressions; any externally reachable URL must
   become configurable HTTPS instead.
4. Run a new scan and require S107, S5958, and S5332 to have zero unreviewed
   findings.

### Task 12: Final repository and live-workflow verification

1. Run all package suites separately using their own pytest configs.
2. Run Ruff, basedpyright, import-linter, build, and wheel smoke exactly as listed
   in `README.md`.
3. Run the real local E2E:

   ```bash
   NANOFAAS_ROOT=/path/to/nanofaas \
   uv run --locked --package nanolab \
   nanolab run packages/nanolab/scenarios-v2/handler-envelope-container.yaml
   ```

4. Run `./scripts/sonar.sh --rm` and assert:
   - zero BLOCKER/HIGH/MEDIUM/LOW findings, or
   - only explicitly reviewed findings with an adjacent source rationale;
   - `.scannerwork/issues.json` contains no untriaged issue.
5. Run `git diff --check` and confirm the only remaining untracked file is any
   pre-existing user-owned plan intentionally left outside the commits.
