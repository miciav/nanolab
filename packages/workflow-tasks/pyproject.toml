# tools/workflow-tasks/pyproject.toml
[project]
name = "workflow-tasks"
version = "0.1.0"
description = "Task execution primitives and workflow event infrastructure."
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.9.2",
    "pydantic-settings>=2.5",
    "httpx>=0.27",
    "multipass-sdk @ git+https://github.com/miciav/multipass-sdk.git@402c406",
    "azure-vm-sdk @ git+https://github.com/miciav/azure-vm-sdk.git@cb159a5",
    "proxmox-sdk @ git+https://github.com/miciav/proxmox-sdk.git@9e20703",
    "shellcraft @ git+https://github.com/miciav/shellcraft.git@b6547835aac88d058810c7b9a592ad442c54ac99",
    "sonata-tasks",
]

[project.scripts]
workflow-tasks-quality = "workflow_tasks.devtools.quality:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
workflow_tasks = ["py.typed"]

[dependency-groups]
dev = [
    "basedpyright>=1.39.3",
    "grimp>=3.14",
    "import-linter>=2.11",
    "pydeps>=3.0.6",
    "pytest>=9.0.3",
    "pytest-cov>=6.0",
    "ruff>=0.15.12",
]

[tool.uv.sources]
sonata-tasks = { workspace = true }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --cov=workflow_tasks --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
omit = ["*/devtools/quality.py"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["F", "SLF"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["F841", "SLF001"]

[tool.basedpyright]
include = ["src/workflow_tasks"]
typeCheckingMode = "basic"
reportMissingTypeStubs = false
reportPrivateUsage = false
